version: '3'

volumes:
  pg_data:

services:
  converter:
    image: anastasiiaborisycheva/pdfconverter
    env_file: .env

  db:
    image: postgres:13
    env_file: .env
    volumes:
      - pg_data:/var/lib/postgresql/data
