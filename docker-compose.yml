version: '3'

volumes:
  pg_data:

services:
  converter:
    build: .
    env_file: .env
    restart: unless-stopped

  db:
    image: postgres:13-alpine
    env_file: .env
    volumes:
      - pg_data:/var/lib/postgresql/data


